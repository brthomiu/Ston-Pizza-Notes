{
      "scripts": {
    "start": "node server/dist/index.js",
    "build": "tsc",
    "heroku-postbuild": "npm install --prefix server && NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client"
  }
}
